{
  "name": "island-house-eds",
  "version": "1.0.0",
  "description": "Island House — Adobe Edge Delivery Services codebase (Universal Editor / xwalk compatible)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx @adobe/aem-cli up",
    "lint": "npx eslint .",
    "lint:fix": "npx eslint . --fix",
    "lint:css": "npx stylelint blocks/**/*.css styles/*.css"
  },
  "devDependencies": {
    "@adobe/aem-cli": "^3.0.0",
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1",
    "stylelint": "^16.3.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
